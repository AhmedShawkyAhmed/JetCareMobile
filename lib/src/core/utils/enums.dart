enum Environment {
  development,
  production,
}

enum Languages {
  ar,
  en,
}

enum Roles {
  client,
  crew,
}

enum OTPTypes{
  register,
  resetPassword,
  resend,
}

enum SuccessType {
  order,
  support,
}

enum OrderStatus{
  assigned,
  unassigned,
  accepted,
  completed,
  canceled,
}
