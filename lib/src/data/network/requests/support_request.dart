class SupportRequest {
  final String name, contact, subject, message;

  SupportRequest({
    required this.name,
    required this.contact,
    required this.subject,
    required this.message,
  });
}
